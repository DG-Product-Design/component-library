export enum ButtonVariants {
  filled = 'filled',
  outline = 'outline',
  flat = 'flat',
}

export enum ButtonColors {
  primary = 'primary',
  secondary = 'secondary',
}

/* export enum ButtonSizes {
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
}; */

export enum ButtonTypes {
  submit = 'submit',
  reset = 'reset',
  button = 'button',
}